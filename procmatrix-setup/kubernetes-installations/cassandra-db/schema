CREATE KEYSPACE IF NOT EXISTS procmatrix WITH REPLICATION = { 'class' : 'SimpleStrategy', 'replication_factor' : 1 };

USE procmatrix;

CREATE TABLE IF NOT EXISTS matrix_data (
    id UUID PRIMARY KEY,
    matrix_data text
);