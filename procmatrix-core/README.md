Procmatrix-Core Project

Overview
The procmatrix-core project contains the core library classes and functionalities required for matrix operations. This project is a dependency for other projects in the procmatrix suite.

DB & Auth. details can be found at below file

```sh
application-core.properties
```